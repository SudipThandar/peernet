# PeerNet Windows Client (Phase 2)

Placeholder. The Windows client will run in the system tray, connect via
Wi-Fi Direct, create a virtual adapter (Wintun), and route traffic through
the same PNTP QUIC tunnel — no manual proxy.

Deferred until Android Phase 1 is stable.
